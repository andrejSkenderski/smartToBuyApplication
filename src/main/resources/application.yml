server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartToBuyApp
    username: postgres
#    password: sorsix
    password: admin
  jpa:
    show-sql: true
    hibernate:
        ddl-auto: validate
  flyway:
    locations:
      - classpath:db/migration